'use client'

// Placeholder while the HALL_EDITOR.md editor lands (tasks 4–5 of the plan).
export default function EditorPage() {
  return (
    <div className="flex h-full items-center justify-center text-sm text-slate-400">
      Hall editor v2 is being rebuilt — check back in a few commits.
    </div>
  )
}
